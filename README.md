# Pocket

Class reminders: wakes you a few minutes before each class.

**<https://vannyz1013.github.io/clock2/>**

---

This repo holds the built site and nothing else. Every file in it is
generated -- editing one here will be overwritten by the next publish.

The source lives in a private repo. Changes are made there to
`src/webpage.h`, then published with `node tools/publish-site.js`.
